<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
    xmlns:ViewModels="clr-namespace:WpfGridControlTest01.ViewModels"
    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" 
    x:Class="WpfGridControlTest01.Views.MainView"
    mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.DataContext>
        <ViewModels:MainViewModel x:Name="viewmodel"/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <dxg:GridControl x:Name="gridctrl0" ItemsSource="{Binding Products}" Grid.Row="1" SelectionMode="Row">
            <dxg:GridControl.View>
                <dxg:TableView x:Name="tableview0"
                    AllowEditing="False"
                    ShowTotalSummary="False"
                    ShowIndicator="False"
                    ShowGroupPanel="False"
                    ShowCheckBoxSelectorColumn="False"
                    IsColumnMenuEnabled="False"
                    IsRowCellMenuEnabled="False"
                    AllowColumnFiltering="False"
                    AllowColumnMoving="False"
                    AllowSorting="True"/>
            </dxg:GridControl.View>
            <!--<dxg:GridColumn>
                <dxg:GridColumn.HeaderTemplate>
                    <DataTemplate>
                        <CheckBox/>
                    </DataTemplate>
                </dxg:GridColumn.HeaderTemplate>
            </dxg:GridColumn>-->
            <dxg:GridColumn FieldName="Chk1" IsSmart="True"/>
            <dxg:GridColumn FieldName="BandWide" IsSmart="True"/>
            <dxg:GridColumn FieldName="Chk2" IsSmart="True"/>
            <dxg:GridColumn FieldName="Freq" IsSmart="True"/>
        </dxg:GridControl>
        <Button x:Name="btnAdd" Content="{Binding TxtBtnAdd}" Click="Button_Click" IsEnabled="{Binding BtnAdd}" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="75" Grid.Row="0"/>
        <Button x:Name="btnDel" Content="{Binding TxtBtnDel}" Click="Button_Click_1" IsEnabled="{Binding BtnDel}" HorizontalAlignment="Left" Margin="90,10,0,0" VerticalAlignment="Top" Width="75" Grid.Row="0"/>
        <Button x:Name="btnReload" Content="{Binding TxtBtnReload}" Click="Button_Click_2" IsEnabled="{Binding BtnReload}" HorizontalAlignment="Left" Margin="170,10,0,0" VerticalAlignment="Top" Width="75" Grid.Row="0"/>
        <Button x:Name="btnApply" Content="{Binding TxtBtnApply}" Click="Button_Click_3" IsEnabled="{Binding BtnApply}" HorizontalAlignment="Left" Margin="389,8,0,0" VerticalAlignment="Top" Width="75" Grid.Row="0"/>
        <Button x:Name="btnModify" Content="{Binding TxtBtnModify}" Click="btnModify_Click" IsEnabled="{Binding BtnModify}" HorizontalAlignment="Left" Margin="250,10,0,0" VerticalAlignment="Top" Width="75" Grid.Row="0"/>        
        <TextBlock x:Name="txtBlockSec"
                   Text="{Binding TimerSec}"
                   FontWeight="Bold"
                   HorizontalAlignment="Right" Margin="0,0,5,0" TextWrapping="Wrap" VerticalAlignment="Center" Grid.Row="0"/>
    </Grid>
</UserControl>
